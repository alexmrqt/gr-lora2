# auto-generated by grc.converter

id: lora2_lora_soft_deinterleaver
label: LoRa Soft Deinterleaver
category: '[lora2]'

parameters:
-   id: SF
    label: SF
    dtype: int
    default: 9
-   id: CR
    label: CR
    dtype: int
    default: 4
-   id: reduced_rate
    label: Reduced rate
    dtype: bool
    default: False

inputs:
-   domain: stream
    dtype: float
    vlen: ${SF-2 if reduced_rate else SF}

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import lora2
    make: lora2.lora_soft_deinterleaver(${SF}, ${CR}, ${reduced_rate})

file_format: 1
