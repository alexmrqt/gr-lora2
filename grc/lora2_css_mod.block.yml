# auto-generated by grc.converter

id: lora2_css_mod
label: CSS Modulator
category: '[lora2]'

parameters:
-   id: M
    label: M
    dtype: int
-   id: interp
    label: Interpolation
    dtype: int

inputs:
-   domain: stream
    dtype: short

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import lora2
    make: lora2.css_mod(${M}, ${interp})

file_format: 1
