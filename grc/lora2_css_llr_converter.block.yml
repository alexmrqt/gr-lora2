# auto-generated by grc.converter

id: lora2_css_llr_converter
label: CSS LLR Converter
category: '[lora2]'

parameters:
-   id: M
    label: 'M'
    dtype: int
    default: 512
-   id: SF
    label: 'SF'
    dtype: int
    default: 9
-   id: true_llr
    label: 'True LLR'
    dtype: bool
    default: False
-   id: sigma_n
    label: 'Noise variance'
    dtype: float
    default: 1.0

inputs:
-   domain: stream
    dtype: float
    vlen: ${M}

outputs:
-   domain: stream
    dtype: float
    vlen: ${SF}

templates:
    imports: import lora2
    make: lora2.css_llr_converter(${M}, ${true_llr}, ${sigma_n})

file_format: 1
