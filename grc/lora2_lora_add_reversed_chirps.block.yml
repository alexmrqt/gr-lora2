# auto-generated by grc.converter

id: lora2_lora_add_reversed_chirps
label: loRa Add Reversed Chirps
category: '[lora2]'

parameters:
-   id: SF
    label: SF
    dtype: int
-   id: interp
    label: Interpolation
    dtype: int
-   id: len_tag_key
    label: Length Tag Key
    dtype: string
-   id: payload_tag_key
    label: Payload Tag Key
    dtype: string
-   id: rev_chirps_tag_key
    label: Rev Chirps Tag Key
    dtype: string

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import lora2
    make: lora2.lora_add_reversed_chirps(${SF}, ${interp}, ${len_tag_key}, ${payload_tag_key},
        ${rev_chirps_tag_key})

file_format: 1
