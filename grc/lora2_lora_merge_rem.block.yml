# auto-generated by grc.converter

id: lora2_lora_merge_rem
label: LoRa Merge Rem.
category: '[lora2]'

parameters:
-   id: SF
    label: SF
    dtype: int
-   id: len_tag_key
    label: Length Tag Key
    dtype: string

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import lora2
    make: lora2.lora_merge_rem(${SF}, ${len_tag_key})

file_format: 1
