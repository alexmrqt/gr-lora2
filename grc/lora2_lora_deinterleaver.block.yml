# auto-generated by grc.converter

id: lora2_lora_deinterleaver
label: LoRa Deinterleaver
category: '[lora2]'

parameters:
-   id: SF
    label: SF
    dtype: int
-   id: CR
    label: CR
    dtype: int
-   id: reduced_rate
    label: Reduced rate
    dtype: bool
    default: false

inputs:
-   domain: stream
    dtype: short

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import lora2
    make: lora2.lora_deinterleaver(${SF}, ${CR}, ${reduced_rate})

file_format: 1
