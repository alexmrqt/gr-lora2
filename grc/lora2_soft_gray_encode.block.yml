# auto-generated by grc.converter

id: lora2_soft_gray_encode
label: Soft Gray Encode
category: '[lora2]'

parameters:
-   id: bpw
    label: Bits per word
    dtype: int
    default: 9

inputs:
-   domain: stream
    dtype: float
    vlen: ${bpw}

outputs:
-   domain: stream
    dtype: float
    vlen: ${bpw}

templates:
    imports: import lora2
    make: lora2.soft_gray_encode(${bpw})

file_format: 1
