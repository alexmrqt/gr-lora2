# auto-generated by grc.converter

id: lora2_lora_soft_dewhiten
label: LoRa Soft Dewhiten
category: '[lora2]'

parameters:
-   id: CR
    label: CR
    dtype: int
-   id: len_tag_key
    label: Length Tag Key
    dtype: string

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import lora2
    make: lora2.lora_soft_dewhiten(${CR}, ${len_tag_key})

file_format: 1
