# auto-generated by grc.converter

id: lora2_tag_delay
label: Tag Delay
category: '[lora2]'

parameters:
-   id: delay
    label: Delay
    dtype: int

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import lora2
    make: lora2.tag_delay(${delay})

file_format: 1
