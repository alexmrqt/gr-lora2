# auto-generated by grc.converter

id: lora2_lora_header_decode
label: LoRa Header Decode
category: '[lora2]'

parameters:
-   id: SF
    label: SF
    dtype: int
-   id: check_crc
    label: Check CRC
    dtype: bool

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: message
    id: hdr

templates:
    imports: import lora2
    make: lora2.lora_header_decode(${SF}, ${check_crc})

file_format: 1
