# auto-generated by grc.converter

id: lora2_count_tags
label: count_tags
category: '[lora2]'

parameters:
-   id: key_filter
    label: Key Filter
    dtype: string

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import lora2
    make: lora2.count_tags(key_filter=${key_filter})

file_format: 1
