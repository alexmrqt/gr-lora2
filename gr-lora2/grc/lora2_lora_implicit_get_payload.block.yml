# auto-generated by grc.converter

id: lora2_lora_implicit_get_payload
label: LoRa Implicit Get Payload
category: '[lora2]'

parameters:
-   id: payload_len
    label: Payload length (bytes)
    dtype: int
-   id: SF
    label: SF
    dtype: int
-   id: CR
    label: CR
    dtype: int
-   id: has_crc
    label: Has CRC
    dtype: bool

inputs:
-   domain: stream
    dtype: short

outputs:
-   domain: stream
    dtype: short

templates:
    imports: import lora2
    make: lora2.lora_implicit_get_payload(${payload_len}, ${SF}, ${CR}, ${has_crc})

file_format: 1
