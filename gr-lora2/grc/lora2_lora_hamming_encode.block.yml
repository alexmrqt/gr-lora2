# auto-generated by grc.converter

id: lora2_lora_hamming_encode
label: LoRa Hamming Encode
category: '[lora2]'

parameters:
-   id: CR
    label: CR
    dtype: int
-   id: len_tag_key
    label: Length Tag Key
    dtype: string

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import lora2
    make: lora2.lora_hamming_encode(${CR}, ${len_tag_key})

file_format: 1
