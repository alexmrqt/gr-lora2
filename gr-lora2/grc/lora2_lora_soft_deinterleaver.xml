<?xml version="1.0"?>
<block>
  <name>LoRa Soft Deinterleaver</name>
  <key>lora2_lora_soft_deinterleaver</key>
  <category>[lora2]</category>
  <import>import lora2</import>
  <make>lora2.lora_soft_deinterleaver($SF, $CR, $reduced_rate)</make>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  <param>
    <name>SF</name>
    <key>SF</key>
    <type>int</type>
  </param>

  <param>
    <name>CR</name>
    <key>CR</key>
    <type>int</type>
  </param>

  <param>
    <name>Reduced rate</name>
    <key>reduced_rate</key>
    <type>bool</type>
  </param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type>float</type>
    <vlen>$SF</vlen>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>out</name>
    <type>float</type>
  </source>
</block>
