# auto-generated by grc.converter

id: lora2_css_llr_converter
label: CSS LLR Converter
category: '[lora2]'

parameters:
-   id: M
    label: M
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: SF
    label: SF
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: true_llr
    label: True LLR
    dtype: bool
-   id: sigma_b
    label: Noise variance
    dtype: float

inputs:
-   domain: stream
    dtype: float
    vlen: ${ M }

outputs:
-   domain: stream
    dtype: float
    vlen: ${ SF }

templates:
    imports: import lora2
    make: lora2.css_llr_converter(${M}, ${true_llr}, ${sigma_b})

file_format: 1
