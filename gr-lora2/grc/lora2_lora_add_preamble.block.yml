# auto-generated by grc.converter

id: lora2_lora_add_preamble
label: LoRa Add Preamble
category: '[lora2]'

parameters:
-   id: pre_len
    label: Preamble length [6-65535]
    dtype: int
    default: '8'
-   id: sync_word
    label: Sync word
    dtype: int
    default: '0x12'
-   id: len_tag_key
    label: Length Tag Key
    dtype: string
    default: '"packet_len"'
-   id: sync_word_tag_key
    label: Sync Word Tag Key
    dtype: string
    default: '"sync_word"'
-   id: payload_tag_key
    label: Payload Tag Key
    dtype: string
    default: '"payload_start"'

inputs:
-   domain: stream
    dtype: short
    vlen: 1

outputs:
-   domain: stream
    dtype: short
    vlen: 1

templates:
    imports: import lora2
    make: lora2.lora_add_preamble(${pre_len}, ${sync_word}, ${len_tag_key}, ${sync_word_tag_key},
        ${payload_tag_key})

file_format: 1
