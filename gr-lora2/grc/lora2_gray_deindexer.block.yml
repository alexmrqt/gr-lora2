# auto-generated by grc.converter

id: lora2_gray_deindexer
label: Gray Deindexer
category: '[lora2]'

parameters:
-   id: M
    label: M
    dtype: int
    default: 9

inputs:
-   domain: stream
    dtype: float
    vlen: ${M}

outputs:
-   domain: stream
    dtype: float
    vlen: ${M}

templates:
    imports: import lora2
    make: lora2.gray_deindexer(${M})

file_format: 1
