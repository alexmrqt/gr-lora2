# auto-generated by grc.converter

id: lora2_lora_preamble_detect
label: LoRa Preamble Detect
category: '[lora2]'

parameters:
-   id: SF
    label: SF
    dtype: int
-   id: thres
    label: Threshold
    dtype: float
    default: 1e-4
-   id: preamble_len
    label: Preamble length [6-65535]
    dtype: int

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import lora2
    make: lora2.lora_preamble_detect(${SF}, ${preamble_len}, thres=${thres})

file_format: 1
