# auto-generated by grc.converter

id: lora2_lora_interleaver
label: LoRa Interleaver
category: '[lora2]'

parameters:
-   id: SF
    label: SF
    dtype: int
-   id: CR
    label: CR
    dtype: int
-   id: reduced_rate
    label: Reduced rate
    dtype: bool
    default: false

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: short

templates:
    imports: import lora2
    make: lora2.lora_interleaver(${SF}, ${CR}, ${reduced_rate})

file_format: 1
