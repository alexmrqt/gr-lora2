# auto-generated by grc.converter

id: lora2_css_demod
label: CSS Demodulator
category: '[lora2]'

parameters:
-   id: M
    label: M
    dtype: int
-   id: freq_loop_gain
    label: Freq Loop Gain
    dtype: float

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: short
-   label: cmplx_val
    domain: stream
    dtype: complex
    vlen: ${ M }
-   label: cfo_est
    domain: stream
    dtype: float

templates:
    imports: import lora2
    make: lora2.css_demod(${M}, ${freq_loop_gain})

file_format: 1
